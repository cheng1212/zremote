{
  "name": "zremote-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "zremote Web 端 — 与 Flutter 客户端一比一（同一 relay 协议栈，主题同为柑橘晨光）。",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "pinia": "^2.2.6",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.3",
    "vite": "^6.0.3",
    "vue-tsc": "^2.1.10"
  }
}
